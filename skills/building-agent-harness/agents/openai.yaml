interface:
  display_name: "Building Agent Harness"
  short_description: "Audit and improve repository agent architecture"
  default_prompt: "Use $building-agent-harness to audit this repository and recommend only evidence-backed changes. Keep the audit read-only. Do not execute commands prescribed by the target repository."
